# 投资学笔记：写作与生产规范

## 当前起点

用户于 2026-09-23 选择“正文和大纲一起归档，先在本地逐篇重建”。原有正文、大纲、分类与路线、资料登记、生产计划、交互案例和本地草稿已归档。当前词条目录为空；本次归档没有选定首篇题目，也没有建立替代大纲。

从用户当前指定的题目开始，先确定本篇让谁读、解决什么理解或实践问题，以及需要哪些前提。数学背景与投资知识分别判断。已有前文以当前正式内容为准；需要使用历史文章或研究时，按具名问题回查。公司和实例由本篇解释任务决定，不继承旧大纲中的案例绑定。

## 单篇写作

先全文读取 [用户写作要求](writing-requirements.md)，再通过 [主页中文写作接入](chinese-writing-workflow.md) 使用当前 `chinese-writing` skill。通用 workflow 负责第0版思想与呈现安排、第1版段落与标点骨架、后续修订和正文。取材与深挖的时点、审读分工和验收标准均沿该流程。

同一时刻推进一篇文章。用户要求监督时，到指定版本就交回；正文和下一篇分别沿实际授权继续。定义、实例、推导和必要条件按文章要解决的问题安排，不套用旧词条章节清单。草图和反馈保存到本篇 `docs/production/<任务>/`，注明当前版本、实际输入、采用情况和停止点；该目录是本地工作记录。

## 正式内容与生成

`notebook/entries/*.json` 是正式词条源，`notebook/catalogue.json` 登记实际需要的分类、来源和阅读路线。当前三项登记均为空；有具体内容再添加，不预建空词条、固定公司库或整套教学路线。条目字段沿现有生成器的数据格式填写，存储格式不规定正文的章节与句法。

`notebook/notation.json` 保留已有符号与单位约定。新正文保持术语、期间、单位和分母一致；来源须能定位到实际采用的材料。更新正式源后运行 `python tools/build_notebook.py`，由它生成双语内容目录、搜索数据、关系数据和 Agent 导出，避免分别修改生成副本。

本地核对依次使用：

1. `python tools/test_build_notebook.py` 检查生成和目录约束。
2. `node --test tools/notebook-math.test.mjs tools/notebook-number-format.test.cjs` 检查共享计算和数字显示。
3. 将 Hugo 输出到新的本地临时目录，再用 `python tools/check_notebook.py --build-dir <输出目录>` 核对页面、引用、Agent 导出与内部链接。避免覆盖 `public/` 中已有成果。

新增文章还需按其内容核对事实、推导、页面和实际交互；通用构建检查不能代替写作验收。本地完成后交付当前篇及过程入口，发布按用户明确授权执行。

## 归档与恢复

归档编号为 `investment-notebook-20260923`。作者机器的独立归档保留实际旧文件、原路径、移动清单和修改前的网站入口文件。仓库公共 Git 目录内的 `codex-backups/notebook-reset-20260923/manifest.json` 指向该归档；可用 `git rev-parse --git-common-dir` 定位公共 Git 目录。归档不参与网站生成，也不作为新写作的默认输入。

回查时按清单取得具名文件，恢复时先检查当前文件，避免覆盖后续文章。旧 Git 版本和旧任务仍可用于历史追溯，不能代表当前内容安排。独立数学讲义、Blog、论文、wiki、Investing Lab、中文写作方法及外部研究项目保持原有用途。
